<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js"></script>
    <script type="text/javascript" src="http://code.jquery.com/jquery-latest.js"></script>
    <script language="javascript">
        function popup_signup() { window.open("signup", "회원가입", "width=450, height=150, left=400, top=200"); }
        function popup_login() { window.open("login", "로그인", "width=450, height=150, left=400, top=200"); }
    </script>
    <style>
        @import url(http://fonts.googleapis.com/earlyaccess/notosanskr.css);
        @import url(//fonts.googleapis.com/earlyaccess/jejugothic.css);
        @import url('https://fonts.googleapis.com/css?family=Baloo+Tammudu|Lobster');
        #header {
            font-family: 'Baloo Tammudu', cursive;
            /*font-family: 'Jeju Gothic', sans-serif;*/
            height: 50px;
            background: #fff;
            box-shadow: 0 0 5px rgba(57, 70, 78, .2);
            z-index: 100;
            padding-top: 5px;
            font-size: 23px;
        }

        #header_btn {
            font-family: 'Noto Sans KR', sans-serif;
            vertical-align: middle;
            float: right;
            padding-right: 1%;
        }

        #sidebar {
            padding: 0;
            margin: 0;
            width: 17%;
            position: absolute;
            top: 50px;
            left: 0;
            padding: 1%;
            border-right: solid 1px #e9eced;
            height: calc(100vh - 55px);
            background: #fff;
            display: flex;
            flex-direction: column;
            background-color: #eee;
        }

        #content {
            padding-left: 18%;
            margin: 1%;
        }

        table,
        td {
            padding: 5px;
        }

        hr {
            margin-right: 2%;
            margin-left: 2%;
        }

        ul {
            /*파비콘으로 리스트 표시하기?*/
            list-style-image: url('http://www.google.com/s2/favicons?domain_url=http://www.naver.com');
        }
    </style>
    <script>
        $( document ).ready( function() {
            var offset = $( '.menu' ).offset();
            $( window ).scroll( function() {
                if ( $( document ).scrollTop() > offset.top ) {
                    $( '.menu' ).addClass( 'fixed' );
                }
                else {
                    $( '.menu' ).removeClass( 'fixed' );
                }
            });
        } );
    </script>
    <title>Home</title>
</head>

<body>
<header class="header">
    <a type="button" onclick="popup_signup();">회원가입</a>
    <a type="button" onclick="popup_login();">로그인</a>
</header>
<section>
    <article class="">
        <div class="menu">
            <input type="text" id="name" name="url_name" placeholder="웹페이지 이름을 입력하세요."/>
            <input type="text" id="address" name="url_address" placeholder="웹페이지 주소를 입력하세요."/>
            <input type="button" id="address_regist" value="등록" onclick="alert('등록되었습니다.')"/>
            <hr />
            <div id="name_list"><%=url_name%></div>
        </div>
    </article>
    <article>
        <div class="content" id="content">

        </div>
    </article>

    <script>
        //$('#login_check').html('');
        $('#login_check').text('로그아웃');
            $(document).ready( function() {
            $('#close').click( function() {
                $('#popup').hide();
            })
        })

        $('#address_regist').click(function() {
            $.ajax({
                url:'',         //"/main/"+id||pw
                dataType:'json',
                type:'POST',
                data:{'name':$('#name').val(),'address':$('#address').val()},
                success:function(result){   //서버가 리턴해준 result가 인자로 온다
                    if(result['result']==true){     //서버가 리턴해준 result값이 true라면(서버측에서 지정함. 예를 들어 로그인을 하고 난 다음 성공적이라고 11을 보내주면 true가 아니라 11이 됨)
                        $('#name_list').html(result['url_name']);
                    }
                }
            })
        })

        $(function () {
            $.ajax({
                url:'',     //불러올 피드
                dataType:'json',
                type:'POST',
                success:function(rss){
                    $(rss).find('item').each(function () {
                        var title=$(this).find('title').text();
                        var content=$(this).find('content').text;
                        var image=$(this).find('image').text();
                        var url=$(this).find('urlId').text();
                        $('#content').after('<form id="useGo_${status.index}"' +'<p>'+title+'<p>'+'<p>'+image+content+'<p>'+'</from>');
                        $('#1').click( function() {
                            $(location).attr('href', url);
                        })
                    })
                }
            })
        })
         //var showWords = function(data) {}
    </script>
</section>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content= "text/html; charset=UTF-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">
    <style>
        th,td {padding:15px;}
        table{border-spacing:10px; margin:auto; text-glign:center;}
    </style>
    <script>
        function delrow(){
            var stable = document.getElementById('stable');
            if(confirm("삭제하시겠습니까?")){
                for(var i=1; i<rows; i++){
                    if(document.getElementsByClassName("tr")[i].cells[0].firstChild.checked){
                        stable.deleteRow(i);
                    }
                }
            }
            return;
        }
    </script>
    <title>웹페이지 목록</title>
</head>

<body>
    <article class="container">
        <div class="page-header">
            <h1>웹페이지 목록</h1>
        </div>

        <form class="form-horizontal" action="">
            <div class="form-group">
                <div>
                    <table id="stable" summary="웹페이지 목록">
                        <thead>
                        <tr>
                            <th></th>
                            <th width="150">이름</th>
                            <th width="800">주소</th>
                        </tr>
                        </thead>
                        <tbody>
                        <%for(var i=0; i < rows; i++){ %>
                        <tr>
                            <td><input type="checkbox"></td>
                            <td><%= name %></td>
                            <td><%= address %></td>
                        </tr>
                        <%}%>
                        </tbody>
                    </table>
                    <div style="text-align:right;">
                        <a class="btn btn-primary" type="button" href="/regist"> 등록</a>
                        <input class="btn btn-danger" type="button" value="삭제" onclick="delrow()">
                    </div>
                </div>
            </div>
        </form>
    </article>
</body>
</html>